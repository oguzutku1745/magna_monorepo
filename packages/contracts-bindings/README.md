# Magna Contract Bindings

This workspace package stores generated Aztec TypeScript bindings.

## Generate

From the repo root:

```bash
npm run codegen:contracts
```

Bindings are generated into `src/` via `aztec codegen`.
