{
  "name": "@magna/core",
  "version": "0.1.0",
  "description": "Dependency-free policy and connector protocol primitives for Magna",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js", "default": "./dist/index.js" }
  },
  "files": ["dist"],
  "engines": { "node": ">=24.12.0" },
  "publishConfig": { "access": "public", "registry": "https://registry.npmjs.org/" },
  "repository": { "type": "git", "url": "git+https://github.com/oguzutku1745/magna_monorepo.git", "directory": "packages/magna-core" },
  "scripts": {
    "build": "node -e \"require('node:fs').rmSync('dist', { recursive: true, force: true })\" && tsc -p tsconfig.json",
    "build:release": "node -e \"require('node:fs').rmSync('dist', { recursive: true, force: true })\" && tsc -p tsconfig.build.json",
    "prepack": "npm run build:release",
    "test": "npm run build && node --test dist/*.spec.js",
    "lint": "echo \"no lint configured\""
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
