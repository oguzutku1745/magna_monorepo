import pinoBrowserDefault from "pino/browser";
import pinoSymbols from "pino/lib/symbols";

export const pino = pinoBrowserDefault;
export const symbols = pinoSymbols;
export default pinoBrowserDefault;
