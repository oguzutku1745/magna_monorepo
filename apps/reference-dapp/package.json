{
  "name": "@magna/reference-dapp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@magna/client": "0.2.0"
  },
  "scripts": {
    "build": "tsc --noEmit --skipLibCheck --module NodeNext --moduleResolution NodeNext --target ES2022 src/login-with-magna.ts",
    "test": "echo \\\"No tests configured\\\"",
    "lint": "npm run build"
  }
}
